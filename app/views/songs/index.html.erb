<h1>Song Index</h1>

<div>
  <% @songs.each do |song| %>

  <p><%= song.artist_name if song.artist %> - <%= link_to song.title, song_path(song) %></p>

  <%= link_to(song) do %>
  <strong><%= @profile.name %></strong>
  <% end %>

  
  <% end %>

</div>


<!-- expect(page).to have_link("Daft Punk - The Grid", href: song_path(@song)) -->
